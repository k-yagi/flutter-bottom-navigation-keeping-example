import 'package:flutter/material.dart';

class NavigatorScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Navigator Screen')
      ),
      body: Center(
        child: const Text('Navigator Screen')
      )
    );
  }
}
